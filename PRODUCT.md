# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是**围坐一桌玩桌游的玩家**，4–5 人，每人一部手机。他们在游戏进行中频繁增减自己面前的 token（幸存者、钱、食物这类），痛点是桌上 token 堆叠混乱、抬头看不清别人还剩多少、要靠口头问。

其中一人承担「开局」角色：创建房间、把二维码或 4 位房间码亮给同桌。这个角色没有额外权限，只是先手。

用户全部在同一张桌子边，面对面；应用不服务远程对局。

## Product Purpose

把桌上物理 token 的清点搬到每个人自己的手机上，让同桌任何人抬头一看就知道全桌的数量与排名。打开网页、扫码、记分，不注册、不安装、不依赖任何自建服务。

方向是**通用桌游计分器**：面向所有桌游玩家，游戏覆盖面持续扩展，《月球殖民地》只是第一款。

成功的样子：一整局游戏里没有人再需要去数别人面前的 token，中途也没有因为掉线而重新报数。

## Positioning

不是通用「记分板 app」，也不是某一款游戏的官方配套。三条别家不能照抄的机制：

- **纯静态站点 + 公共 MQTT 中继**：零后端、零账号、零密钥，托管在 GitHub Pages，扫码即进。不是「暂时没做后端」，是产品前提。
- **每人只写自己的分**：他人视角在协议层面只读，结构上排除了替别人改数。
- **游戏由定义文件驱动**：一款游戏记哪些 token、怎么排名、何时出局，全部写在定义文件里，界面不为单款游戏写特例。这是「通用计分器」能成立的技术前提。

## Operating Context

- 线下桌边，手机竖屏、单手操作，环境常偏暗；一局 45–90 分钟，屏幕反复点亮。
- 网络混杂：同桌成员可能有的连 Wi-Fi、有的用蜂窝，跨运营商是常态。必须在中国大陆可用，默认中继列表含 `broker-cn.emqx.io`。
- 入房路径只有两条：相机扫二维码打开链接，或在首页手输 4 位房间码。应用不内置扫码器。
- 刷新、切后台、临时断网是正常流程的一部分，不是异常分支：本地持久化自己与他人的最后快照，回前台自动重连并拉快照。

## Capabilities and Constraints

**已实现并确认的能力**

- 身份：机器人头像 + 昵称；`playerId` 跨会话稳定，存 localStorage，无账号。
- 房间：4 位房间码（去除 0/O/1/I 等易混字符）、分享链接、二维码；无房主、无踢人。
- 计分：每位玩家一组计数器，±1 / ±5 步进，快捷行动（采矿 +4 钱、耕作 +4 食物）；可查看任意他人的分数，只读。
- 排行：按主计数器排序（MCB 为幸存者）；归零标记「殖民地失败」并广播。
- 同步：MQTT 中继直传，`join / beat / bye / msg` 四类消息，8 秒心跳、26 秒静默判离线、LWT 兜底异常断开；三个中继并连冗余 + 消息 id 去重。
- 设置：中继服务器列表可改，连接诊断页显示各中继状态。
- PWA：可安装、离线可用。

**技术约束（已验证，不得回退）**

- 无自建后端，GitHub Pages 托管，Vue Router hash 模式。
- **不使用 WebRTC**：运营商 NAT 为对称型，跨 Wi-Fi/蜂窝必然打洞失败，而唯一解法 TURN 已无可用的免费公共服务（Open Relay 公开凭证全部失效）。这是协议层结论，不是代码缺陷。
- 公共中继无 SLA：入房慢、偶发断连必须设计进界面，而不是当作错误弹窗。
- 单条状态消息约 260 字节、往返约 400 毫秒；房间规模按 1–5 人设计。

**明确不做（2026-09-21 确认）**

功能面停在当前形态，后续扩张的是游戏覆盖面，不是功能数量。以下均已决定不做：

- 结算页、可分享长图；
- 事件流、操作历史、撤销 / 重做；
- 单机模式（一部手机记全桌）；
- 账号体系、云端战绩、内置扫码器、多语言、自动裁判规则、语音视频。

**未决**

- 游戏库的规模与组织方式（列表、搜索、分类、首次引导）尚未确定，新增第二款游戏时才需要定论。

## Brand Commitments

- 名称《局分》GameTally，中文名优先。
- 玩家标识是自绘图形，不用真人头像或字母缩写，且必须不依赖色相就能区分 —— 色觉差异的玩家要能认出自己那一行。具体形态见 DESIGN.md。
- 界面语言为简体中文，不做国际化。
- **游戏封面使用 BoardGameGeek 图片 CDN 的真实封面作为入口**（不落库到仓库，加载失败退回自绘占位）。README 中「不使用出版社的封面与卡图」一句与现状冲突，以本条为准，README 需更正。
- token 图标依据出版社官方规则书重绘为矢量，不直接截取原图。
- 引用的桌游资料必须注明来源（BGG 或出版社官方页）。

## Evidence on Hand

- **真实游戏数据**：《月球殖民地》BGG #425549。设计师 Donald X. Vaccarino、出版社 Rio Grande Games、2025 年、1–5 人、45–90 分钟、起始 30 人 / 4 钱 / 4 食物、胜负只看幸存者 —— 均取自 BGG 与 Rio Grande Games 官方页（2026-09-20 查阅），见 `src/games/moon-colony-bloodbath.ts`。
- **真实封面图**：BGG CDN `pic8638247`（含 2x 与缩略三档）。
- **自绘矢量 token**：`src/assets/games/moon-colony-bloodbath/{person,money,food}.svg`。
- **实测网络数据**：`broker-cn.emqx.io` 连接 1.9s、`broker.emqx.io` 1.5s、`broker.hivemq.com` 3.3s，往返均约 380ms；`test.mosquitto.org` 连接失败已移出默认列表。
- **线上部署**：https://miot.github.io/game-tally/ ，v1.0.0 已发布并验证。
- **测试**：Vitest 多端同步用例（`src/stores/room.spec.ts`）、Playwright 端到端（`tests/e2e/room.spec.ts`、`sync.spec.ts`）。
- **不存在、后续不得编造**：用户数量、评价与推荐语、历史战绩数据、除 MCB 外任何游戏的规则数据（新增游戏必须先查 BGG 与官方规则书）。

## Product Principles

1. **每人只写自己的分。** 他人视角只读是硬规则，任何新功能都不得让一个人替另一个人改数。
2. **无后端、无账号、扫码即用。** 任何需要注册、服务器、密钥或付费服务的方案都不进这个产品。
3. **网络不可靠是常态。** 中继慢、对端离线、刷新、切后台之后，界面仍要讲清「现在是什么状态、数据什么时候更新的」。
4. **新增游戏只写定义文件。** 界面由游戏定义驱动；一款新游戏若要求改界面，先扩展定义模型，而不是加单款游戏的特例分支。
5. **桌边可读优先。** 大号读数、不小于 44px 的触控目标、主要操作在下半屏 —— 这是昏暗桌边单手使用的产物，不是风格选择。

## Accessibility & Inclusion

- 触控目标不小于 44px，主要操作位于下半屏拇指可达区。
- 安全区适配（顶部刘海与底部手势条），支持横竖屏。
- 状态与变化不得仅靠色相传达：在线、离线、只读、出局、增减方向都必须另有可读的记号。具体手段见 DESIGN.md。
- 读数使用等宽数字，保证桌边远距离与快速扫视的可读性。
- 仅简体中文，不做国际化 —— 这是明确的产品边界，不是无障碍缺口。
