# 验证报告 —《局分》GameTally v1

- 审查时间：2026-09-20 22:30
- 审查对象：仓库首个可发布版本（脚手架、游戏定义、房间同步、UI、PWA、测试、部署工作流）
- 审查方式：本地自动执行（无 CI），逐项命令与结果如下

## 1. 需求字段完整性

| 字段 | 内容 | 状态 |
|------|------|------|
| 目标 | 手机浏览器手动记录桌游 token，房间内互看分数，无后端，GitHub Pages 部署 | ✅ |
| 范围 | 首款游戏《月球殖民地》，只记幸存者/钱/食物；Trystero 同步；切换查看他人只读；大陆网络默认；不做单机记全桌 | ✅ 与用户 2026-09-20 决策一致 |
| 交付物 | 代码、docs/plan-v1.md、README、测试、部署工作流、本报告 | ✅ |
| 审查要点 | 同步正确性、只读约束、边界（上下限、撤销、刷新恢复）、移动端可用性、主题一致性 | ✅ 见第 3 节 |

## 2. 本地验证执行结果

| 项目 | 命令 | 结果 |
|------|------|------|
| 单元 / 集成测试 | `pnpm test` | 6 个文件 31 个用例通过 |
| 类型检查 | `pnpm type-check` | 通过（vue-tsc --build） |
| 静态检查 | `pnpm lint:check` | 通过 |
| 格式 | `pnpm format:check` | 通过 |
| 构建 | `pnpm build` | 通过；首屏 JS 114 KB（gzip 45 KB），MQTT 客户端 383 KB 按需分块 |
| 端到端（单设备） | `pnpm test:e2e` | 3 个用例通过：创建/记分/撤销/刷新恢复/邀请、链接进入先起名、无效房间码回首页 |
| 端到端（真实网络双设备） | `E2E_NETWORK=1 pnpm test:e2e tests/e2e/sync.spec.ts` | 通过（18.8 s）：两个独立上下文经公共 MQTT 中继入房、互见分数、只读视角、离线检测 |
| 视觉核对 | Playwright 截图 iPhone 13 视口 | 首页 / 房间 / 排行榜 / 邀请 / 设置 五张核对，主题变量全局作用域修正后配色一致 |

## 3. 覆盖矩阵

| 行为 | 测试位置 |
|------|----------|
| 游戏定义自洽（主计数器唯一、初始值范围、快捷行动引用） | src/games/games.spec.ts |
| 房间码生成 / 校验 / 归一化 | src/utils/room-code.spec.ts |
| 版本合并规则 | src/sync/messages.spec.ts |
| 传输层入房 / 定向发送 / 离开 / 序列化 | src/sync/memory-transport.spec.ts |
| 两人互见、广播合并、迟到快照、上下限、撤销分组、离线保留、冒充忽略、缓存恢复、排行与出局、只读切换 | src/stores/room.spec.ts |
| 计数器按钮增量、只读无按钮、上下限禁用 | src/components/TokenCounter.spec.ts |
| 完整用户流程（手机视口） | tests/e2e/room.spec.ts |
| 真实 Trystero 传输层 | tests/e2e/sync.spec.ts（需联网，默认跳过） |

## 4. 评分

**技术维度**
- 代码质量：92 —— 单写者模型简单可靠；传输层抽象清晰；Trystero 实现经真实网络验证。扣分：昵称长度、房间码字母表等常量散落在组件与工具中，可集中。
- 测试覆盖：90 —— 核心逻辑与关键流程均有自动化；真实网络用例需手动开启。
- 规范遵循：93 —— 中文注释、官方脚手架配置、Prettier/ESLint 通过。

**战略维度**
- 需求匹配：95 —— 九项决策逐条落实；三项 token 用自绘图形展示，加减按钮 44px 以上。
- 架构一致：92 —— 游戏定义数据驱动，新增游戏不改 UI；传输层可替换。
- 风险评估：85 —— 跨运营商蜂窝网络 NAT 穿透无法在本地验证；公共中继无 SLA；GitHub Pages 大陆访问速度不可控。

**综合评分：91 / 100 —— 建议：通过**

## 5. 已知风险与补偿计划

| 风险 | 说明 | 补偿 |
|------|------|------|
| 大陆真机网络 | 本机出口为香港，STUN/中继可达性只是间接证据 | 已提供设置页切换策略、自定义 STUN/TURN 与连接诊断；首次真机使用后按诊断结果调整默认列表 |
| 跨运营商直连失败 | 需要 TURN | 设置页可填 TURN；README 已说明 |
| 同一人两台设备 | playerId 按设备生成，换机后会成为新玩家 | v1 接受；后续可加「接管」流程 |
| 公共中继撞房 | 房间码 4 位约 105 万组合，appId 隔离 | v1 接受；可扩展到 5 位或加房间密码 |

## 6. 决策结论
综合评分 91，建议「通过」，可提交并推送触发 GitHub Pages 部署。
